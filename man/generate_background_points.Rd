% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_background.R
\name{generate_background_points}
\alias{generate_background_points}
\title{Generate background points for invasive species analysis}
\usage{
generate_background_points(
  occ_sf,
  study_area = NULL,
  n_bg = 10000,
  method = c("random", "restricted", "target_group"),
  buffer_dist = NULL,
  bbox_expand = 0.1,
  target_points = NULL
)
}
\arguments{
\item{occ_sf}{sf POINT. Invasive species occurrence locations.}

\item{study_area}{Optional polygon (sf or terra SpatVector). If NULL, a
bounding box around occurrences is used when method = "random".}

\item{n_bg}{Number of background points.}

\item{method}{One of \code{c("random","restricted","target_group")}.}

\item{buffer_dist}{Buffer distance around occurrences (same units as CRS),
required when \code{method = "restricted"}.}

\item{bbox_expand}{Proportion to expand the occurrence bounding box in each
direction when \code{study_area = NULL} and \code{method = "random"}.}

\item{target_points}{Optional sf POINT for \code{method = "target_group"}.}
}
\value{
sf POINT with background locations.
}
\description{
Generate background points representing available environment.

Logic:
\itemize{
\item If \code{study_area} is provided:
\itemize{
\item method = "random": sample uniformly inside study_area;
\item method = "restricted": sample inside a buffer around occurrences
(optionally intersected with study_area).
}
\item If \code{study_area} is NULL:
\itemize{
\item method = "random": sample inside an expanded bounding box of occurrences;
\item method = "restricted": sample inside a buffer around occurrences.
}
}
}
